*~
*.nav
*.log
*.aux
*.out
*.toc
*.snm
*.bbl
*.blg
article/.auctex-auto

# Nix build results and direnv's cache.
result
result-*
.direnv/

# Build products of article/Makefile. refs.bib is generated from the
# filecontents block at the top of SmallLatticeReps.tex, so it should never be
# edited or committed directly.
article/SmallLatticeReps.pdf
article/inputs/refs.bib

# Python
__pycache__/
*.pyc
